<li>
  <%= gravatar_for user, size: Settings.gravatar.size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t("label.form.user.delete"), user, method: :delete,
      data: {confirm: t("label.form.user.confirm")} %>
  <% end %>
</li>
